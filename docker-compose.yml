services:
  discord-moderation-sync:
    build: .
    container_name: discord-moderation-sync
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    ports:
      - "${NATION_OAUTH_PORT:-8080}:${NATION_OAUTH_PORT:-8080}"
    volumes:
      - ./data:/app/data
