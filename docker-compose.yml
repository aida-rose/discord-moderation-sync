services:
  discord-moderation-sync:
    build: .
    container_name: discord-moderation-sync
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    ports:
      - "${MC_VERIFY_API_PORT:-8765}:${MC_VERIFY_API_PORT:-8765}"
    volumes:
      - ./data:/app/data
